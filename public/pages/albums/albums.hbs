<section class="section"> 
    <h1>Только для тебя</h1>
    {{>collections}}
</section>
{{#if this.user}}
    <section class="section section--loved">
        <h1>Любимые альбомы</h1>
        <div>
            {{#each this.loved}}
                {{>album-card}}
            {{/each}}
        </div>
    </section>
{{/if}}
<section class="section section--recommendations">
    <h1>Рекомендации</h1>
    <div>
        {{#each this.recommendations}}
            {{>album}}
        {{/each}}
    </div>
</section>
